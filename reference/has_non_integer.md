# Check for non-integer values in a matrix

This function checks whether a numeric matrix contains any non-integer
values.

## Usage

``` r
has_non_integer(mat)
```

## Arguments

- mat:

  A numeric matrix.

## Value

A logical value: `TRUE` if any element is non-integer, otherwise
`FALSE`.
